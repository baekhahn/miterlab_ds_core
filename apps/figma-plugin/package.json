{
  "name": "@miterlab/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sh -c 'if command -v esbuild >/dev/null 2>&1; then esbuild src/code.ts --bundle --platform=browser --format=iife --outfile=code.js; else ../../node_modules/.pnpm/esbuild@0.27.4/node_modules/esbuild/bin/esbuild src/code.ts --bundle --platform=browser --format=iife --outfile=code.js; fi'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.110.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.6.3"
  }
}
