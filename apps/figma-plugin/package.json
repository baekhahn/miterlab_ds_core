{
  "name": "@miterlab/figma-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --platform=browser --format=iife --outfile=code.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.110.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.6.3"
  }
}
